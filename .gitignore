# Generated by Cargo
# will have compiled files and executables
debug/
target/

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

**/.DS_Store
**/node_modules

engine/dist

private.md

backup

# Solana test validator ledger (local dev only)
test-ledger/
**/test-ledger/
.anchor/test-ledger/

# ── Decoupled from the desktop client build ───────────────────────────────────
# The Solana contract (chain/) and the legacy chain-coupled frontend (app/) are
# no longer part of the standalone 3D engine + desktop client (client/desktop/).
# See docs/plan/CHAIN_DECOUPLE_PLAN.md.
# NOTE: app/ and chain/ are still git-tracked from earlier commits; .gitignore
# only affects UNtracked files. To actually stop tracking them (keeps files on
# disk):  git rm -r --cached app chain
/app/
/chain/

# Client build artifacts (PWA frontends under client/)
client/*/dist/
client/*/dev-dist/