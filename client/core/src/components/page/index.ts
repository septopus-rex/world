/**
 * The shared 2D page stack — see ./README.md for the shape of a page and the
 * rules that come with it. Import from `@core/components/page`.
 */
export { PageProvider, usePages, usePageStack, usePageActive } from './PageStack';
export { PageHost } from './PageHost';
export type { PageSpec, PageVariant, PageSize, PagesApi, ConfirmOptions, PageStackState } from './types';
