/**
 * Render - 2D Render
 *
 * @fileoverview
 *  1. 2D render from `3D STD` data
 *
 * @author Fuu
 * @date 2025-04-23
 */

const reg={
    name:"rd_two",
    type:'render',
}

const config={
    
}

const self={
    hooks:{
        reg:()=>{return reg},
    }
};

export default {
    hooks:self.hooks,
    construct:(id)=>{

    },
    auto:(id)=>{

    },
}