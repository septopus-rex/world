/* 
*  Module block, single block
*  @auth [ Fuu ]
*  @creator Fuu
*  @date 2025-04-23
*  @functions
*  1.
*/

const reg={
    name:"rd_two",
    type:'render',
}

const config={
    
}

const self={
    hooks:{
        reg:()=>{return reg},
    }
};

export default {
    hooks:self.hooks,
    construct:(id)=>{

    },
    auto:(id)=>{

    },
}