/**
 * Effects -  mesh color
 *
 * @fileoverview
 *  1. mesh color change effect
 *
 * @author Fuu
 * @date 2025-09-16
 */


const Color = (todo, active , ck) => {
    const {meshes,config} = todo;

    for(let i=0;i<todo.length;i++){
        const row=todo[i];


    }
}

export default Color;