/**
 * Effects -  mesh scale
 *
 * @fileoverview
 *  1. mesh scale effect
 *
 * @author Fuu
 * @date 2025-09-08
 */


const Scale = (todo, active , ck) => {
    const {meshes,config} = todo;

    for(let i=0;i<todo.length;i++){
        const row=todo[i];


    }
}

export default Scale;