/**
 * Effects -  mesh opacity
 *
 * @fileoverview
 *  1. mesh opacity change effect
 *
 * @author Fuu
 * @date 2025-09-16
 */


const Opacity = (todo, active , ck) => {
    const {meshes,config} = todo;

    for(let i=0;i<todo.length;i++){
        const row=todo[i];


    }
}

export default Opacity;