/**
 * Effects -  mesh morph
 *
 * @fileoverview
 *  1. mesh morph effect
 *
 * @author Fuu
 * @date 2025-09-16
 */


const Morph = (todo, active , ck) => {
    const {meshes,config} = todo;

    for(let i=0;i<todo.length;i++){
        const row=todo[i];


    }
}

export default Morph;